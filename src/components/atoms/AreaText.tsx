import React, { FC } from 'react';
import '../../styles/atoms/AreaText.scss';

type Props = { }

const AreaText: FC<Props> = () => {
  return (
    <div>AreaText</div>
  );
};

export default AreaText;